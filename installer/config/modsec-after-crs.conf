# Mundix Security 360 — tuning avaliado DEPOIS das regras do CRS.
#
# Coloque aqui ajustes que dependem do CRS já ter sido carregado, por exemplo
# desligar uma regra específica que gere falso-positivo no painel:
#   SecRuleRemoveById 949110
#
# Mantido vazio por padrão. Gerenciado fora da UI para evitar lockout —
# veja dashboard/backend/app/services/waf.py.

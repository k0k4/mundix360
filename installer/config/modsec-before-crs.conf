# Mundix Security 360 — exclusões avaliadas ANTES das regras de ataque do CRS.
#
# Coloque aqui o tuning que precisa rodar antes do Core Rule Set, por exemplo:
#   SecRuleRemoveById 920350
#   SecRuleUpdateTargetById 942100 "!ARGS:senha"
#
# Mantido vazio por padrão (identidade limpa do appliance). Gerenciado fora da
# UI para evitar lockout — veja dashboard/backend/app/services/waf.py.
